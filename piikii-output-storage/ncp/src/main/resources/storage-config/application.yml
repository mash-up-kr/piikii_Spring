ncp:
  key:
    access: ${NCP_API_ACCESS_KEY}
    secret: ${NCP_API_SECRET_KEY}
  bucket:
    name: ${NCP_BUCKET_NAME}
    path: ${NCP_BUCKET_DEFAULT_PATH}
    folder:
      place-folder: ${NCP_BUCKET_FOLDER_PLACE}
      room-folder: ${NCP_BUCKET_FOLDER_ROOM}
